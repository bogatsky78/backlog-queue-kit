---
allowed-tools: Bash(bin/backlog:*)
description: Відкладене — статус Later
---

Відкладене (`Later`):

!`bin/backlog task list -s Later --plain --sort ordinal`

Сюди задача потрапляє лише за словом власника. Сама по собі вона звідси не
виходить — підняти в `To Do` теж вирішує власник.
